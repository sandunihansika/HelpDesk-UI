export enum TextBoxTypes {
  Text = 'text',
  Password = 'password',
  Email = 'email'
}

export enum StatusCodes {
  Success = 200,
  Unauthorized = 401
}

export enum UserType {
  AdminUser = 1,
  HelpDeskUser = 5
}

